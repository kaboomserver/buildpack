{
  "name": "Kaboom",
  "env": {
    "PROXY_KEY": {
      "description": "Private key for proxy server",
      "required": true
    },
    "SCHEMATIC_KEY": {
      "description": "Private key for schematic repository",
      "required": true
    }
  }
}
