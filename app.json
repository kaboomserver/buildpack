{
  "name": "Kaboom.pw",
  "env": {
    "REMOTE_KEY": {
      "description": "Private key for reverse SSH",
      "required": true
    },
    "SYNC_KEY": {
      "description": "Private key for data sync",
      "required": true
    }
  }
}
